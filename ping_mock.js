/**
 * Root Proxy Runner cho ping_mock.js:
 * Cho phép chạy "node ping_mock.js <lệnh>" ngay từ thư mục gốc của dự án StrollerApp
 * mà không bắt buộc phải cd vào server/
 */
require('./server/ping_mock');
